services:
  searxng:
    image: searxng/searxng:latest
    container_name: scrape-searxng
    restart: unless-stopped
    mem_limit: ${SEARXNG_MEM_LIMIT:-0}
    ports:
      - "${SEARXNG_PORT:-9810}:8080"
    volumes:
      - ./searxng:/etc/searxng:rw
    environment:
      - SEARXNG_BASE_URL=http://localhost:${SEARXNG_PORT:-9810}/
    networks:
      - scrape-network
    healthcheck:
      test: ["CMD", "wget", "-q", "--spider", "http://localhost:8080/healthz"]
      interval: 10s
      timeout: 5s
      retries: 5

  api:
    build:
      context: ./api
      dockerfile: Dockerfile
    container_name: scrape-api
    restart: unless-stopped
    mem_limit: ${API_MEM_LIMIT:-0}   # 0 = unlimited (default). Set a value in .env to cap Playwright/Firefox memory growth.
    ports:
      - "${API_PORT:-9811}:8000"
    volumes:
      - ./frontend:/app/static:ro
      - ./data:/app/data:rw
    environment:
      - SEARXNG_URL=http://searxng:8080
      - 'SEARCH_SOURCES=${SEARCH_SOURCES:-["searxng","duckduckgo","startpage","brave","bing"]}'
      - OLLAMA_HOST=${OLLAMA_HOST:-http://host.docker.internal:11434}
      - OLLAMA_MODEL=${OLLAMA_MODEL:-gpt-oss:20b}
      - CACHE_DIR=/app/data
      - CACHE_TTL_SEARCH=${CACHE_TTL_SEARCH:-1800}
      - CACHE_TTL_CONTENT=${CACHE_TTL_CONTENT:-86400}
      - PLAYWRIGHT_MAX_CONTEXTS=${PLAYWRIGHT_MAX_CONTEXTS:-3}
      - PLAYWRIGHT_RECYCLE_AFTER=${PLAYWRIGHT_RECYCLE_AFTER:-200}
    extra_hosts:
      - "host.docker.internal:host-gateway"
    depends_on:
      searxng:
        condition: service_healthy
    networks:
      - scrape-network
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/api/health"]
      interval: 10s
      timeout: 5s
      retries: 5

  mcp:
    build:
      context: ./mcp
      dockerfile: Dockerfile
    container_name: scrape-mcp
    restart: unless-stopped
    mem_limit: ${MCP_MEM_LIMIT:-0}
    ports:
      - "${MCP_PORT:-9812}:8000"
    environment:
      - API_URL=http://api:8000
      - OLLAMA_HOST=${OLLAMA_HOST:-http://host.docker.internal:11434}
      - OLLAMA_MODEL=${OLLAMA_MODEL:-gpt-oss:20b}
    extra_hosts:
      - "host.docker.internal:host-gateway"
    depends_on:
      api:
        condition: service_healthy
    networks:
      - scrape-network

networks:
  scrape-network:
    driver: bridge
